Seq-\/queue is simple tool to keep requests to be executed in order.

As we known, Node.\+js codes run in asynchronous mode and the callbacks are unordered. But sometimes we may need the requests to be processed in order. For example, in a game, a player would do some operations such as turn right and go ahead. And in the server side, we would like to process these requests one by one, not do them all at the same time.

Seq-\/queue takes the responsibility to make the asynchronous, unordered processing flow into serial and ordered. It\textquotesingle{}s simple but not a repeated wheel.

Seq-\/queue is a FIFO task queue and we can push tasks as we wish, anytime(before the queue closed), anywhere(if we hold the queue instance). A task is known as a function and we can do anything in the function and just need to call {\ttfamily task.\+done()} to tell the queue current task has finished. It promises that a task in queue would not be executed util all tasks before it finished.

Seq-\/queue add timeout for each task execution. If a task throws an uncaught exception in its call back or a developer forgets to call {\ttfamily task.\+done()} callback, queue would be blocked and would not execute the left tasks. To avoid these situations, seq-\/queue set a timeout for each task. If a task timeout, queue would drop the task and notify develop by a \textquotesingle{}timeout\textquotesingle{} event and then invoke the next task. Any {\ttfamily task.\+done()} invoked in a timeout task would be ignored.


\begin{DoxyItemize}
\item Tags\+: node.\+js
\end{DoxyItemize}\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2901}{}\doxysection{Installation}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2901}

\begin{DoxyCode}{0}
\DoxyCodeLine{npm install seq-\/queue}

\end{DoxyCode}
\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2902}{}\doxysection{Usage}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2902}

\begin{DoxyCode}{0}
\DoxyCodeLine{var seqqueue = require('seq-\/queue');}
\DoxyCodeLine{}
\DoxyCodeLine{var queue = seqqueue.createQueue(1000);}
\DoxyCodeLine{}
\DoxyCodeLine{queue.push(}
\DoxyCodeLine{  function(task) \{}
\DoxyCodeLine{    setTimeout(function() \{}
\DoxyCodeLine{      console.log('hello ');}
\DoxyCodeLine{      task.done();}
\DoxyCodeLine{    \}, 500);}
\DoxyCodeLine{  \}, }
\DoxyCodeLine{  function() \{}
\DoxyCodeLine{    console.log('task timeout');}
\DoxyCodeLine{  \}, }
\DoxyCodeLine{  1000}
\DoxyCodeLine{);}
\DoxyCodeLine{}
\DoxyCodeLine{queue.push(}
\DoxyCodeLine{  function(task) \{}
\DoxyCodeLine{    setTimeout(function() \{}
\DoxyCodeLine{      console.log('world\string~');}
\DoxyCodeLine{      task.done();}
\DoxyCodeLine{    \}, 500);}
\DoxyCodeLine{  \}}
\DoxyCodeLine{);}

\end{DoxyCode}
\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2903}{}\doxysection{API}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2903}
\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2904}{}\doxysubsection{seqqueue.\+create\+Queue(timeout)}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2904}
Create a new queue instance. A global timeout value in ms for the new instance can be set by {\ttfamily timeout} parameter or use the default timeout (3s) by no parameter.\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2905}{}\doxysubsection{queue.\+push(fn, ontimeout, timeout)}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2905}
Add a task into the queue instance. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2906}{}\doxysubsubsection{Arguments}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2906}

\begin{DoxyItemize}
\item fn(task) -\/ The function that describes the content of task and would be invoke by queue. {\ttfamily fn} takes a arguemnt task and we {\itshape must} call task.\+done() to tell queue current task has finished.
\item ontimeout() -\/ Callback for task timeout.
\item timeout -\/ Timeout in ms for {\ttfamily fn}. If specified, it would overwrite the global timeout that set by {\ttfamily create\+Queue} for {\ttfamily fn}.
\end{DoxyItemize}\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2907}{}\doxysubsection{queue.\+close(force)}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2907}
Close the queue. A closed queue would stop receiving new task immediately. And the left tasks would be treated in different ways decided by {\ttfamily force}. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2908}{}\doxysubsubsection{Arguments}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2908}

\begin{DoxyItemize}
\item force -\/ If true, queue would stop working immediately and ignore any tasks left in queue. Otherwise queue would execute the tasks in queue and then stop.
\end{DoxyItemize}\hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2909}{}\doxysection{Event}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2909}
Seq-\/queue instances extend the Event\+Emitter and would emit events in their life cycles. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2910}{}\doxysubsection{\textquotesingle{}timeout\textquotesingle{}(totask)}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2910}
If current task not invoke task.\+done() within the timeout ms, a timeout event would be emit. totask.\+fn and totask.\+timeout is the {\ttfamily fn} and {\ttfamily timeout} arguments that passed by {\ttfamily queue.\+push(2)}. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2911}{}\doxysubsection{\textquotesingle{}error\textquotesingle{}(err, task)}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2911}
If the task function (not callbacks) throws an uncaught error, queue would emit an error event and passes the err and task informations by event callback arguments. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2912}{}\doxysubsection{\textquotesingle{}closed\textquotesingle{}}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2912}
Emit when the close(false) is invoked. \hypertarget{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2913}{}\doxysubsection{\textquotesingle{}drained\textquotesingle{}}\label{md_Backend_nodejs_node_modules_seq_queue_README_autotoc_md2913}
Emit when close(true) is invoked or all tasks left have finished in closed status. 